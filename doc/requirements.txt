# What doc/.venv holds. `npm run docs` uses that venv when it is there and
# falls back to whatever sphinx-build is on PATH, which builds the same pages
# in Sphinx's own theme.
#
# Pinned exactly, because the image builds the guide with -W: warnings are
# errors there, and a new Sphinx, MyST or docutils is free to add one -- a
# deprecation, a stricter cross-reference -- that fails a build nothing in
# this repository changed. docutils is pinned for the same reason although
# nothing here names it. Move these together, and build with -W before
# committing the move.
sphinx==9.1.0
myst-parser==5.1.0
sphinx-rtd-theme==3.1.0
sphinx-intl==2.4.0
docutils==0.22.4
